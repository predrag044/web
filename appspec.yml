version: 0.0
os: linux

files:
        - source: /
          destination: /tmp

permissions:
        - object: /tmp/start.sh
          owner: root
          group: root
          mode: 777
          type:
            - file

hooks:
        ApplicationStart:
                - location: /tmp/start.sh
                  timeout: 30 
                  runas: root
