version: 0.2
phases:
        install:
                commands:
                        - npm install
        build:
                commands:
                        - echo "Nothing to build as this is njs!"

artifacts:
        name: $(AWS_REGION)-$(date +%Y-%m-%d) 
        files:
                - app.js
                - package.json
                - views/*
                - public/*
                - bin/*
                - routes/* 
